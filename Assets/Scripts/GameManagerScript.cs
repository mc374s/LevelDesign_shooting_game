using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManagerScript : MonoBehaviour {

    //public static Vector3 leftBottom = Camera.main.ViewportToWorldPoint(new Vector3(0, 0, 3)), rightTop = Camera.main.ViewportToWorldPoint(new Vector3(1, 1, 3));

    // Use this for initialization
    void Start () {
        //leftBottom = Camera.main.ViewportToWorldPoint(new Vector3(0, 0, 3));
        //rightTop = Camera.main.ViewportToWorldPoint(new Vector3(1, 1, 3));
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
